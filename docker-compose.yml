version: '3.8'

services:
  siliconflow-proxy:
    build: .
    container_name: siliconflow-proxy
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - PORT=3000
      - NODE_ENV=production

